// 导出主要的 Provider
export { RefineProvider } from "./providers";

// 导出资源配置工具
export {
	resources,
	autoCollectedResources,
	resourcesMap,
	getResourceConfig,
	resourceNames,
} from "./resources";

// 导出提供者配置
export { dataProviders } from "./providers/data-provider";
export { routerProvider } from "./providers/router-provider";
